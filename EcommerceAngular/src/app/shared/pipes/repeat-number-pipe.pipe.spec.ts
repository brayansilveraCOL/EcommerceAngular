import { RepeatNumberPipePipe } from './repeat-number-pipe.pipe';

describe('RepeatNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
